<Application x:Class="POSApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:POSApp"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Color x:Key="PrimaryColor">#FF2D89EF</Color>
        <Color x:Key="SecondaryColor">#FF191616</Color>
        <Color x:Key="AccentColor">#FF00B294</Color>
        <Color x:Key="TextColor">#FFFFFFFF</Color>

        <!-- Brushes (what controls actually use) -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}"/>

    </Application.Resources>
</Application>
